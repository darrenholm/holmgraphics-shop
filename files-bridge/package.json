{
  "name": "holmgraphics-files-bridge",
  "version": "1.1.0",
  "description": "Files bridge for Holm Graphics Shop — exposes the L: drive (client artwork storage) to the web app over HTTP.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
